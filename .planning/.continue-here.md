---
context: default
phase: ad-hoc (outside GSD phase — backend wiring session)
task: 7
total_tasks: 14
status: paused
last_updated: 2026-06-14T12:30:23.948Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions.

- [ ] CONSTRAINT: **Credentials missing** — `js/__env.js` has empty `SUPABASE_URL` and `SUPABASE_ANON_KEY`. All Supabase reads/writes silently use a stub. Fill this FIRST before testing anything.
- [ ] CONSTRAINT: **Schema not migrated** — `supabase/migrations/001_schema.sql` was updated this session (contacts table added) but must be run in the Supabase SQL Editor manually. The seed.sql must also be run.
- [ ] CONSTRAINT: **elvora_updated-3.html is a PROTOTYPE** — do not take features from it. The canonical codebase is the separate HTML files (index/shop/about/contact/lookbook.html) using Alpine.js + Supabase.

**Do not proceed until all boxes are checked.**

<current_state>
Ad-hoc session requested by user to "split elvora_updated-3.html into pages and add Supabase backend."

Finding: the project already had a complete multi-page architecture from prior GSD phases 01–03. The monolithic file was a prototype/mockup, not the real app.

Session added the missing backend pieces:
- `netlify/functions/ai-advisor.js` — Claude API proxy (security: keeps key server-side)
- `js/cart.js` — localStorage cart with Alpine.store
- Updated `netlify.toml` — functions dir, /api/* redirect, esbuild
- Added `contacts` table to `supabase/migrations/001_schema.sql`
- Extracted `assets/logo.png` from base64

**No HTML pages were modified** — remaining tasks 10–12 below need code changes.
</current_state>

<completed_work>
Tasks done this session:
1. ✅ Audited existing file structure — confirmed multi-page arch already exists
2. ✅ `netlify/functions/ai-advisor.js` — Claude haiku-4-5 proxy, CORS, error handling
3. ✅ `js/cart.js` — localStorage + Alpine.store('cart'), replaces TODO stubs
4. ✅ `netlify.toml` — functions = "netlify/functions", /api/* redirect, esbuild bundler
5. ✅ `supabase/migrations/001_schema.sql` — appended contacts table with RLS
6. ✅ `assets/logo.png` — extracted from base64 in elvora_updated-3.html
</completed_work>

<remaining_work>
**HUMAN ACTIONS REQUIRED FIRST:**
- [ ] Task 7: Fill `js/__env.js` with real Supabase credentials:
  ```js
  window.__ENV = { SUPABASE_URL: 'https://xxx.supabase.co', SUPABASE_ANON_KEY: 'eyJ...' };
  ```
  → Get from: Supabase Dashboard → Settings → API
- [ ] Task 8: Run `supabase/migrations/001_schema.sql` in Supabase SQL Editor
- [ ] Task 9: Run `supabase/seed.sql` in Supabase SQL Editor
- [ ] Task 13: Add `ANTHROPIC_API_KEY` to Netlify environment variables

**CODE CHANGES STILL NEEDED:**
- [ ] Task 10: Update `contact.html` — replace `setTimeout` stub with real Supabase insert:
  ```js
  const { error } = await supabase.from('contacts').insert({ name, email, message });
  ```
- [ ] Task 11: Add `<script type="module" src="/js/cart.js"></script>` to all HTML pages **before** `components.js`:
  - index.html, shop.html, about.html, contact.html, lookbook.html, product.html, cart.html
- [ ] Task 12: Update AI advisor `sendAI()` calls to POST `/api/ai-advisor` instead of direct `https://api.anthropic.com/v1/messages`
  - Files to check: any page/component that has `sendAI()` or calls Anthropic directly
- [ ] Task 14: Deploy to Netlify and verify all pages + AI advisor + cart flow
</remaining_work>

<decisions_made>
- **Cart strategy**: localStorage + Alpine.store (no Supabase for guest cart). Simple, no auth required, works for assessment scope.
- **AI proxy**: Netlify function (not Supabase Edge Function) — keeps everything on Netlify, simpler config, follows CLAUDE.md spec.
- **Model**: `claude-haiku-4-5-20251001` per CLAUDE.md (cheapest vision-capable model).
- **No HTML restructuring**: existing pages are already well-structured; only gaps were filled.
</decisions_made>

<blockers>
- **Credentials**: js/__env.js empty → user must add Supabase keys (blocking for all DB features)
- **Migration not run**: schema changes sit in file only → user must run in SQL Editor
- **API key**: ANTHROPIC_API_KEY not in Netlify env → AI advisor returns 503 (non-blocking for rest of site)
</blockers>

## Required Reading (in order)
1. `js/__env.js` — check if credentials have been filled since this handoff
2. `supabase/migrations/001_schema.sql` — understand the full schema before touching DB
3. `netlify/functions/ai-advisor.js` — understand the proxy before wiring frontend calls
4. `js/cart.js` — understand Alpine.store('cart') API before adding to pages

## Infrastructure State
- **Supabase project**: initialized (`supabase/config.toml` exists), not migrated yet
- **Netlify**: `netlify.toml` configured with functions; not yet deployed
- **CSS**: `css/style.css` is Tailwind v4 compiled — run `npm run build:css` if adding new Tailwind classes
- **Local dev**: no dev server running; open HTML files directly or use `npx serve .`

<context>
User had a deadline pressure and wanted the project completed. The main discovery was that the architecture was already built (phases 01-03 done). The session filled in the critical missing backend pieces. The user still needs to do credential setup before the live Supabase features work. Once credentials + schema + seed are in place, the remaining code tasks (10-12) are small and targeted.

Next session should start by checking if the user has filled credentials, then move to code tasks 10-12 in order.
</context>

<next_action>
Start by verifying `js/__env.js` — does it have real Supabase credentials?
- If YES → run schema + seed, then proceed to Task 10 (contact form)
- If NO → ask user to add credentials first, provide step-by-step instructions
</next_action>
